export class Document {
  clickCount: number;
  created: string;
  customized: boolean;
  qrCode: string;
  shortId: string;
  url: string;
  userId: string;
  // tslint:disable-next-line: variable-name
  _id: string;
}
